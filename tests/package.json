{
  "name": "ckt-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "node helpers/sync-utils.cjs",
    "test": "node node_modules/vitest/vitest.mjs run --reporter=verbose --no-color --pool=forks",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
